import React from "react";

function App() {
  const message = "Hello, React!";

  return (
    <div>
      <h1>{message}</h1>
      <p>{"This text is also a string literal."}</p>
    </div>
  );
}

export default App;
